# ImageCaption
An automated image captioning system that generates meaningful natural language descriptions by integrating a CNN-based image encoder with an LSTM/Attention-based decoder. The model is trained and evaluated on the Flickr8k dataset using BLEU scores and qualitative analysis.
